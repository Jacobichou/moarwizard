<%= form_for Message.new do |f| %>
<div class="row">
   <div class="large-12 columns">
      <%= render 'shared/error_messages', object: f.object %>
   </div>
</div>
<div class="row">
   <div class="large-12 columns">
      <div class="field">
         <%= collection_select(:message, :recipient_id, User.where("role = ? OR role = ?", 60, 66, 30, 50), :id, :email) %>
         <%= f.text_area :subject, placeholder: "Subject Line" %>
         <%= f.text_area :content, placeholder: "Compose new message..." %>
      </div>
      <%= f.submit "Send", class: "button success" %>

   </div>
</div>
<% end %>


